import {View} from 'react-native';
import FAB from '../atoms/FAB';

function Home(): React.JSX.Element {
  return (
    <View>
      <FAB onPress={() => {}} />
    </View>
  );
}

export default Home;
