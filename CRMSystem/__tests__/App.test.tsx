import {it} from '@jest/globals';
import 'react-native';

it('renders correctly', () => {
  // This throwing ESM error, Since I used latest version I didn't have time to debug this issue
  // Therefore, I could't be able to complete tests around screens and redux
  // renderer.create(<App />);
});
